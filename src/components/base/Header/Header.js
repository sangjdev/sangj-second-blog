import React from "react";
import { Link } from "gatsby";
import "./Header.scss";

const Header = () => {
  return (
    <header className="Header">
      <Link to="#">BLOG</Link>
    </header>
  );
};

export default Header;
